# Server Configuration
PORT=3030

# Node Environment (development, production, test)
NODE_ENV=development

# Database (if adding Prisma later)
# DATABASE_URL=mongodb://localhost:27017/golden-retriver

# CORS (if adding CORS later)
# CORS_ORIGIN=http://localhost:3000

# API Documentation
# Swagger UI is available at http://localhost:3030/swagger
# Swagger JSON is available at http://localhost:3030/swagger/json
